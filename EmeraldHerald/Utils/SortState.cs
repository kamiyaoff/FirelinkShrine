namespace FirelinkShrine.Utils {
    public enum SortState {
        NameAsc,
        NameDesc,
        StatusAsc,
        StatusDesc,
        PriorityAsc,
        PriorityDesc,
        CreatedAsc,
        CreatedDesc,
        DeadlineAsc,
        DeadlineDesc
    }
}
